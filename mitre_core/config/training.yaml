defaults:
  - _self_

epochs: 100
batch_size: 256
learning_rate: 0.001
weight_decay: 1e-4

optimizer: "adamw"
scheduler: "cosine_annealing"

early_stopping_patience: 20

seed: 42
device: "cuda"

checkpoint_dir: "checkpoints"
log_dir: "logs"

